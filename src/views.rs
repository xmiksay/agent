// Obsolete: server-rendered views have been replaced by the Vue SPA in
// `frontend/`. This file is no longer referenced from `main.rs` and exists
// only because the original `rm` was denied. Safe to delete.
